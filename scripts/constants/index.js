export const CustomCssProperties = Object.freeze({
    GRID_DIM: "--grid-dim",
    GRID_CELL_SIZE: "--grid-cell-size",
    GRID_GAP: "--grid-gap",
});

export const CSSSelectors = Object.freeze({
    BOARD: "#board",
    CELLS: ".cell",
    TILES: ".tile",
});